import React from 'react';

const Blogs = () => {
    return (
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100vh' }}>
            <h1 style={{ textAlign: 'center' }}>Blogs are coming soon</h1>
        </div>
    );
};

export default Blogs;